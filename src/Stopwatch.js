import React from 'react';

class Stopwatch extends React.Component {
    render() {
        return (
            <div id="buttons">
                <button>Start</button>
                <button>Stop</button>
            </div>
        );
    }
}

export default Stopwatch;